# Gen-AI
Generative text summarization is a process where AI models create concise summaries of a text by generating new sentences, rather than just extracting existing ones. It uses large language models (LLMs) to analyze the source document, understand its context, and then produce a new
